#!/bin/bash -p
set -Eeuo pipefail
unset BASH_ENV ENV NODE_OPTIONS NODE_PATH SUPERVISOR_TOKEN

readonly STATUS_FILE=/run/antigravity-ha/browser-auth-status.json

if [[ ! -r "${STATUS_FILE}" ]]; then
  jq --null-input '{status: "unavailable"}'
  exit 1
fi

jq . "${STATUS_FILE}"
