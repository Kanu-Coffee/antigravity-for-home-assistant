#!/bin/bash -p
set -Eeuo pipefail
unset BASH_ENV ENV NODE_OPTIONS NODE_PATH SUPERVISOR_TOKEN

if (( $# != 0 )); then
  printf 'ha-read-broker-runtime: command-line arguments are not supported\n' >&2
  exit 64
fi

exec /usr/bin/node /usr/local/share/antigravity-ha/ha-read-broker.mjs
